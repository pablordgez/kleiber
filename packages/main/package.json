{
  "name": "@kleiber/main",
  "private": true,
  "version": "0.0.0",
  "main": "../../dist/main/index.js",
  "scripts": {
    "build": "echo \"Built from root via electron-vite\"",
    "clean": "rimraf ../../dist/main",
    "lint": "eslint .",
    "test": "vitest run packages/main/src/**/*.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@kleiber/shared": "workspace:*",
    "electron-log": "^5.4.3",
    "electron-store": "^10.1.0",
    "node-pty": "^1.0.0",
    "yaml": "^2.8.1"
  }
}
